name: test

on: 

  push:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  

  build:

    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: sleep 5

      - name: Run a muiti-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
